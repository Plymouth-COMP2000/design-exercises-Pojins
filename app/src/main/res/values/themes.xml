<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme -->
    <style name="Base.Theme.SavoryRestaurant" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Disable Material 3 button tint -->
        <item name="materialButtonStyle">@style/NoTintM3Button</item>
    </style>

    <style name="Theme.SavoryRestaurant" parent="Base.Theme.SavoryRestaurant" />

    <!-- Custom button style with NO tint -->
    <style name="NoTintM3Button" parent="Widget.Material3.Button">
        <item name="backgroundTint">@null</item>
    </style>

</resources>
