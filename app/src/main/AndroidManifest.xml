<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SavoryRestaurant">

        <!-- FIRST PAGE (Homepage) -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <!-- User Selection Page -->
        <activity android:name=".UserSelectionActivity" />

        <!-- Guest Sign in -->
        <activity android:name=".GuestSigninActivity" />

        <!-- Guest Register-->
        <activity android:name=".GuestRegisterActivity" />

        <!-- Login Page -->
        <activity android:name=".LoginActivity" />

        <!-- Profile-->
        <activity android:name=".ProfileActivity" />

        <!-- Guest Dashboard -->
        <activity android:name=".GuestDashboardActivity" />

        <!-- Staff Dashboard -->
        <activity android:name=".DashboardActivity" />

        <!-- Menu Management -->
        <activity android:name=".MenuActivity" />

        <!-- Menu Edit -->
        <activity android:name=".MenuEditActivity" />

        <!-- Reservation Edit -->
        <activity android:name=".ReservationActivity" />

        <!-- Guest Reservation  -->
        <activity android:name=".GuestReservationActivity" />

        <!-- Guest Menu  -->
        <activity android:name=".GuestMenuActivity" />

        <!-- Guest Reservation Add  -->
        <activity android:name=".GuestAddReservationActivity" />








    </application>

</manifest>
